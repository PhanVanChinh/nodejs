mixin search(keyword)
    form(id="form-search")
            .input-group
            input(type="text" placeholder="Nhập từ khóa" name="keyword" value=keyword)
            button(
                type="submit") Tìm